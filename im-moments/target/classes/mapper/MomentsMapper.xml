<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzlew.mapper.MomentsMapper">

    <!--插入后自动把数据库自增 ID 回填到实体类的 id 字段-->
    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into moments(user_id, username, avatar, content)
        values (#{userId}, #{username}, #{avatar}, #{content})
    </insert>

    <select id="listByNew" resultType="com.zzzlew.domain.vo.MomentsVO">
        select *
        from moments
        <where>
            is_deleted = 0
            <if test="lastId != null and lastId > 0">
                and id &lt; #{lastId}
            </if>
        </where>
        order by id DESC
        limit #{pageSize}
    </select>

    <select id="getById" resultType="com.zzzlew.domain.vo.MomentsVO">
        select *
        from moments
        where id = #{momentId}
    </select>

    <select id="selectByIds" resultType="com.zzzlew.domain.vo.MomentsVO">
        select * from moments where id in
        <foreach item="item" collection="missingIds" separator="," open="(" close=")">
            #{item}
        </foreach>
    </select>

    <select id="comments" resultType="com.zzzlew.domain.vo.MomentsCommentsVO">
        select *
        from moment_comments
        where moment_id = #{momentId}
        and (parent_id = 0 or parent_id is null)
        and is_deleted = 0
        order by like_count DESC
    </select>

    <select id="listByHot" resultType="com.zzzlew.domain.vo.MomentsVO">
        select *
        from moments
        where is_deleted = 0
        order by like_count DESC
    </select>

    <update id="like">
        update moments
        set like_count = like_count + #{i}
        where id = #{momentId}
    </update>

    <update id="updateCommentCount">
        update moments
        set comment_count = comment_count + #{increment}
        where id = #{momentId}
    </update>

    <insert id="publishComment" useGeneratedKeys="true" keyProperty="id">
        insert into moment_comments(moment_id, user_id, username, avatar, content)
        values (#{momentId}, #{userId}, #{username}, #{avatar}, #{content})
    </insert>

    <insert id="publishCommentReply" useGeneratedKeys="true" keyProperty="id">
        insert into moment_comments(moment_id, user_id, username, avatar, content, parent_id, reply_to_user_id, reply_to_username)
        values (#{momentId}, #{userId}, #{username}, #{avatar}, #{content}, #{parentId}, #{replyToUserId}, #{replyToUsername})
    </insert>

    <select id="commentReplies" resultType="com.zzzlew.domain.vo.MomentsCommentsVO">
        select *
        from moment_comments
        where parent_id = #{commentId}
        and is_deleted = 0
        order by publish_time ASC
    </select>

    <update id="likeComment">
        update moment_comments
        set like_count = like_count + #{increment}
        where id = #{commentId}
    </update>


</mapper>