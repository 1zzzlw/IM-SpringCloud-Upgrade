# IM-SpringCloud 项目依赖层次结构文档

## 📋 项目概览

**技术栈版本**
- Java: 17
- Spring Boot: 3.2.10
- Spring Cloud: 2023.0.3
- Spring Cloud Alibaba: 2023.0.1.2
- Maven: 多模块项目

**项目结构**
```
IM-SpringCloud (父项目)
├── im-common (公共模块组)
│   ├── common-model (数据模型)
│   ├── common-base (基础工具和常量)
│   ├── common-redis (Redis和JWT工具)
│   ├── common-storage (MinIO存储工具)
│   ├── common-web (Web层统一封装)
│   └── common-core (兼容聚合模块)
├── im-api (Feign客户端接口定义)
├── im-gateway (API网关 - Spring Cloud Gateway)
├── im-auth (认证授权服务)
├── im-chat (聊天消息服务)
├── im-social (社交关系服务)
├── im-content (内容管理服务)
├── im-moments (朋友圈服务 - 暂未实现)
└── im-server (Netty WebSocket服务器)
```

---

## 🏗️ 一、公共模块 (im-common)

### 1. common-model
**定位**: 纯数据模型层，提供POJO、DTO、VO类

**依赖关系**:
```
common-model
├── fastjson (1.2.83) - JSON序列化
├── jackson-annotations - Spring JSON注解
└── lombok (编译时) - 简化代码
```

**特点**: 无业务逻辑，只包含数据类定义

---

### 2. common-base
**定位**: 基础工具层，提供常量、枚举、异常类

**依赖关系**:
```
common-base
└── lombok (编译时) - 简化代码
```

**特点**: 零外部依赖（除lombok），最轻量级

---

### 3. common-redis
**定位**: Redis操作和JWT工具封装

**依赖关系**:
```
common-redis
├── common-base ✅ - 基础工具
├── spring-boot-starter - Spring Boot基础
├── spring-boot-starter-data-redis - Redis操作
├── jaxb-api (2.3.1) ⚠️ - Java 17兼容JWT
└── jjwt (0.9.1) - JWT生成和解析
```

**关键说明**:
- ⚠️ **JAXB依赖**: 必需！jjwt 0.9.1在Java 11+环境需要此依赖
- 提供JwtUtil工具类供全局使用

---

### 4. common-storage
**定位**: MinIO文件存储封装

**依赖关系**:
```
common-storage
├── spring-boot-starter - Spring Boot基础
├── minio (8.5.1) - MinIO客户端
└── spring-boot-starter-web - MultipartFile支持
```

**特点**: 独立存储工具，不依赖其他common模块

---

### 5. common-web
**定位**: Web层统一封装（拦截器、异常处理、Swagger等）

**依赖关系**:
```
common-web
├── common-model ✅ - 数据模型（Result、DTO）
├── common-redis ✅ - JWT验证和Redis操作
│   └── common-base ✅ (传递依赖)
├── common-storage ✅ - 文件上传功能
├── knife4j (4.4.0) - API文档
├── fastjson (1.2.83) - JSON处理
├── spring-boot-starter-amqp - RabbitMQ
└── hutool-all (5.8.40) - 工具类库
```

**功能**:
- 用户拦截器 (UserInterceptor) - 从请求头提取用户信息
- 全局异常处理
- Swagger文档配置
- 文件上传通用接口

---

### 6. common-core
**定位**: 兼容层聚合模块（向后兼容旧代码）

**依赖关系**:
```
common-core (聚合器)
├── common-model ✅
├── common-base ✅
├── common-redis ✅
└── common-storage ✅
```

**说明**: 一次性引入所有基础功能，适合需要完整功能但不需要Web层的模块

---

## 🚀 二、微服务模块

### 1. im-gateway (API网关)
**定位**: 统一入口，路由转发、认证鉴权、令牌刷新

**依赖关系**:
```
im-gateway
├── common-model ✅ - UserBaseDTO等数据类
├── common-redis ✅ - JWT解析和Redis操作
│   ├── common-base ✅ (传递)
│   ├── jaxb-api (2.3.1) ⚠️ (传递)
│   └── jjwt (0.9.1) (传递)
├── hutool-all (5.8.40) - 工具类
├── spring-boot-starter-data-redis-reactive ⚠️ - 响应式Redis
├── spring-cloud-starter-gateway - 网关核心
├── spring-cloud-starter-alibaba-nacos-discovery - 服务发现
└── spring-cloud-starter-loadbalancer - 负载均衡
```

**关键说明**:
- ⚠️ **响应式Redis**: 必须用reactive版本，普通redis会导致Bean冲突
- ⚠️ **JAXB**: 通过common-redis传递依赖获得（已修复）
- **不依赖common-web**: 网关使用WebFlux，不是传统MVC
- **认证策略**: excludePaths (完全跳过) / refreshPaths (长期token) / 默认 (短期token)

---

### 2. im-auth (认证授权服务)
**定位**: 用户注册、登录、验证码生成

**依赖关系**:
```
im-auth
├── common-web ✅ - 完整Web功能
│   ├── common-model ✅ (传递)
│   ├── common-redis ✅ (传递)
│   │   ├── common-base ✅ (传递)
│   │   └── jaxb-api ⚠️ (传递)
│   └── common-storage ✅ (传递)
├── im-api ✅ - Feign客户端
├── mysql-connector-java (8.0.33) - 数据库
├── mybatis-spring-boot-starter (3.0.3) - ORM
├── spring-cloud-starter-alibaba-nacos-discovery - 服务发现
├── kaptcha (2.3.2) - 图形验证码
├── spring-boot-starter-aop - 切面支持
└── feign-okhttp - HTTP客户端优化
```

**功能**:
- 用户注册/登录
- JWT短期/长期token生成
- 图形验证码生成

---

### 3. im-chat (聊天消息服务)
**定位**: 消息收发、聊天记录、AI聊天助手

**依赖关系**:
```
im-chat
├── common-web ✅ - 完整Web功能
├── im-api ✅ - Feign客户端
├── mysql-connector-java (8.0.33) - 数据库
├── mybatis-spring-boot-starter (3.0.3) - ORM
├── spring-cloud-starter-alibaba-nacos-discovery - 服务发现
├── feign-okhttp - HTTP客户端
├── spring-ai-starter-model-ollama (1.1.2) ⚠️ - 本地AI模型
└── spring-ai-starter-model-zhipuai (1.1.2) ⚠️ - 智谱AI
```

**特点**:
- ⚠️ 集成AI大模型能力
- 支持多种AI提供商

---

### 4. im-social (社交关系服务)
**定位**: 好友关系、群组管理、黑名单

**依赖关系**:
```
im-social
├── common-web ✅ - 完整Web功能
├── im-api ✅ - Feign客户端
├── mysql-connector-java (8.0.33) - 数据库
├── mybatis-spring-boot-starter (3.0.3) - ORM
├── spring-cloud-starter-alibaba-nacos-discovery - 服务发现
└── feign-okhttp - HTTP客户端
```

**功能**: 标准Web服务，处理用户社交关系

---

### 5. im-content (内容管理服务)
**定位**: 会话管理、消息记录

**依赖关系**:
```
im-content
├── common-web ✅ - 完整Web功能
├── im-api ✅ - Feign客户端
├── mysql-connector-java (8.0.33) - 数据库
├── mybatis-spring-boot-starter (3.0.3) - ORM
└── spring-cloud-starter-alibaba-nacos-discovery - 服务发现
```

**特点**: 无Feign-okhttp依赖（可考虑添加以保持一致性）

---

### 6. im-moments (朋友圈服务)
**定位**: 朋友圈功能（待开发）

**依赖关系**:
```
im-moments
└── (空) - 模块已创建但未实现
```

**状态**: 预留模块，pom.xml为空

---

### 7. im-server (Netty WebSocket服务器)
**定位**: 长连接服务器，实时消息推送

**依赖关系**:
```
im-server
├── common-model ✅ - 数据模型
├── common-base ✅ - 基础工具
├── common-redis ✅ - JWT验证和Redis
│   ├── jaxb-api (2.3.1) ⚠️ (传递)
│   └── jjwt (0.9.1) (传递)
├── netty-all (4.1.94.Final) - Netty框架
├── redisson-spring-boot-starter (3.21.3) - 分布式锁
├── fastjson (1.2.83) - JSON处理
├── spring-boot-starter-amqp - RabbitMQ
└── hutool-all (5.8.40) - 工具类
```

**关键说明**:
- ⚠️ **不是Spring MVC**: 使用Netty独立启动，不需要common-web
- **RabbitMQ集成**: 消息分发到多个WebSocket连接
- **Redisson**: 分布式会话管理和锁

---

### 8. im-api (Feign客户端接口)
**定位**: 服务间调用接口定义

**依赖关系**:
```
im-api
├── common-model ✅ - 共享数据模型
├── spring-cloud-starter-openfeign - Feign客户端
├── spring-cloud-starter-loadbalancer - 负载均衡
└── jackson-annotations - JSON注解
```

**特点**: 轻量级，仅定义接口，不包含实现

---

## 📊 三、依赖树形图

```
微服务层（依赖 common-web 或 common-core）
├── im-auth
│   ├── common-web ✅
│   │   ├── common-model ✅
│   │   ├── common-redis ✅
│   │   │   ├── common-base ✅
│   │   │   ├── redis
│   │   │   ├── jaxb-api (2.3.1) ⚠️
│   │   │   └── jjwt (0.9.1)
│   │   ├── common-storage ✅
│   │   │   ├── minio
│   │   │   └── spring-web
│   │   ├── knife4j
│   │   ├── rabbitmq
│   │   └── hutool
│   ├── im-api ✅
│   ├── mysql + mybatis
│   ├── nacos-discovery
│   ├── kaptcha
│   ├── aop
│   └── feign-okhttp
│
├── im-chat
│   ├── common-web ✅ (同上)
│   ├── im-api ✅
│   ├── mysql + mybatis
│   ├── nacos-discovery
│   ├── feign-okhttp
│   ├── spring-ai-ollama ⚠️
│   └── spring-ai-zhipuai ⚠️
│
├── im-social
│   ├── common-web ✅ (同上)
│   ├── im-api ✅
│   ├── mysql + mybatis
│   ├── nacos-discovery
│   └── feign-okhttp
│
├── im-content
│   ├── common-web ✅ (同上)
│   ├── im-api ✅
│   ├── mysql + mybatis
│   └── nacos-discovery
│
├── im-gateway (响应式架构)
│   ├── common-model ✅
│   ├── common-redis ✅
│   │   ├── common-base ✅
│   │   ├── redis
│   │   ├── jaxb-api (2.3.1) ⚠️
│   │   └── jjwt (0.9.1)
│   ├── hutool
│   ├── spring-data-redis-reactive ⚠️
│   ├── spring-cloud-gateway
│   ├── nacos-discovery
│   └── loadbalancer
│
├── im-server (Netty独立服务)
│   ├── common-model ✅
│   ├── common-base ✅
│   ├── common-redis ✅
│   │   ├── redis
│   │   ├── jaxb-api (2.3.1) ⚠️
│   │   └── jjwt (0.9.1)
│   ├── netty-all
│   ├── redisson
│   ├── fastjson
│   ├── rabbitmq
│   └── hutool
│
├── im-moments
│   └── (空 - 未实现)
│
└── im-api (接口定义)
    ├── common-model ✅
    ├── openfeign
    ├── loadbalancer
    └── jackson-annotations
```

---

## ⚠️ 四、已修复的关键问题

### 1. ✅ im-gateway: ReactiveRedisTemplate Bean缺失
**问题**: 使用了阻塞式Redis依赖导致Bean不兼容
**解决**: 替换为 `spring-boot-starter-data-redis-reactive`

### 2. ✅ im-gateway: Redis序列化配置类型错误
**问题**: 泛型类型不匹配
**解决**: 正确使用泛型和类型转换

### 3. ✅ im-server: RabbitAdmin Bean缺失
**问题**: NettyWebSocketServer需要RabbitAdmin但未配置
**解决**: 创建RabbitMQConfig配置类

### 4. ✅ JWT解析失败: NoClassDefFoundError javax/xml/bind/DatatypeConverter
**问题**: jjwt 0.9.1在Java 11+需要JAXB依赖
**解决**: 在common-redis中添加jaxb-api (2.3.1)，所有使用JWT的模块自动继承

### 5. ✅ 初始化接口返回401: 用户信息未传递
**问题**: 初始化接口在excludePaths中，Gateway完全跳过认证
**解决**: 移动到refreshPaths，使用长期token验证并传递user-info

---

## 🔍 五、潜在问题和建议

### 🟡 观察项（非强制修复）

#### 1. Fastjson版本安全警告
**位置**: 所有使用fastjson的模块
**现状**: 使用fastjson 1.2.83
**说明**: 
- 该版本已修复多数已知安全漏洞
- 如果未来有安全要求，可考虑升级到2.x或替换为Jackson
- **当前可用**: 不影响功能

#### 2. im-content缺少feign-okhttp
**位置**: im-content/pom.xml
**现状**: 其他微服务（im-auth, im-chat, im-social）都添加了feign-okhttp
**说明**:
- feign-okhttp提供连接池和性能优化
- 保持一致性建议添加
- **影响较小**: 不添加也能正常工作

#### 3. MySQL驱动版本
**位置**: 所有数据库服务
**现状**: mysql-connector-java 8.0.33
**说明**:
- 当前版本稳定可用
- Spring Boot 3.2.10推荐使用com.mysql:mysql-connector-j
- **可选升级**: 为保持一致性

#### 4. jjwt版本较旧
**位置**: common-redis
**现状**: jjwt 0.9.1 (需要jaxb-api补丁)
**说明**:
- 新版本jjwt (0.11.x+) 原生支持Java 11+，不需要jaxb-api
- **当前已修复**: 添加jaxb-api后完全可用
- **可选升级**: 升级到0.11.5+可移除jaxb-api依赖

#### 5. Netty版本
**位置**: im-server
**现状**: netty-all 4.1.94.Final
**说明**:
- 当前版本稳定
- 可考虑升级到4.1.100+（性能优化）
- **非必需**: 现有版本工作正常

---

## ✅ 六、项目健康状态总结

### 整体评估: 良好 ✅

**优点**:
- ✅ 模块分层清晰，职责明确
- ✅ 依赖管理规范，使用父pom统一版本
- ✅ JWT在Java 17环境已完全修复
- ✅ Gateway响应式架构正确
- ✅ 所有启动失败问题已解决

**当前可用**:
- ✅ 注册功能正常
- ✅ 登录功能正常
- ✅ 初始化接口（会话、消息、好友）正常
- ✅ Gateway认证和路由正常
- ✅ Netty WebSocket服务正常
- ✅ RabbitMQ消息分发正常

**建议（可选）**:
- 🟡 考虑统一添加feign-okhttp到所有微服务
- 🟡 长期可考虑升级jjwt到0.11.x（移除jaxb-api补丁）
- 🟡 长期可考虑fastjson迁移到Jackson（安全考虑）

---

## 📝 七、关键技术说明

### JWT认证流程
1. **短期Token** (1小时): 默认API请求使用
2. **长期Token** (30天): 初始化接口使用，配置在refreshPaths
3. **Token刷新**: Gateway自动刷新即将过期的短期token

### Redis使用场景
- **Token存储**: 用户token映射
- **会话管理**: 在线状态和设备信息
- **分布式锁**: Redisson（im-server）
- **响应式操作**: Gateway使用ReactiveRedisTemplate

### RabbitMQ消息流转
1. im-server接收WebSocket消息
2. 发布到RabbitMQ Topic Exchange (imTopicExchange)
3. 订阅者接收并推送到目标连接

---

**文档生成时间**: 2026-06-03  
**项目版本**: 1.0-SNAPSHOT  
**文档状态**: 全面扫描完成 ✅
