<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzlew.mapper.WalletMapper">

    <select id="selectByUserIdForUpdate" resultType="com.zzzlew.domain.entity.Wallet">
        SELECT * FROM wallet WHERE user_id = #{userId} FOR UPDATE
    </select>

    <update id="deductBalance">
        UPDATE wallet 
        SET balance = balance - #{amount} 
        WHERE user_id = #{userId} AND balance >= #{amount}
    </update>

    <update id="addBalance">
        UPDATE wallet 
        SET balance = balance + #{amount} 
        WHERE user_id = #{userId}
    </update>

    <insert id="insertRecord">
        INSERT INTO wallet_record (user_id, amount, type, business_id, before_balance, after_balance, remark)
        VALUES (#{userId}, #{amount}, #{type}, #{businessId}, #{before}, #{after}, #{remark})
    </insert>

    <insert id="initWallet">
        INSERT IGNORE INTO wallet (user_id, balance, freeze_balance) VALUES (#{userId}, 0, 0)
    </insert>

    <select id="selectRecords" resultType="com.zzzlew.domain.vo.WalletRecordVO">
        SELECT id, amount, type, business_id, before_balance, after_balance, remark, create_time
        FROM wallet_record
        WHERE user_id = #{userId}
        <if test="type != null and type != 0">
            AND type = #{type}
        </if>
        ORDER BY create_time DESC
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <select id="countRecords" resultType="long">
        SELECT COUNT(*) FROM wallet_record
        WHERE user_id = #{userId}
        <if test="type != null and type != 0">
            AND type = #{type}
        </if>
    </select>

</mapper>