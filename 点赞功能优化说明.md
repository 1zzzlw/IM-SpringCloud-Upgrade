# 点赞功能优化说明

## 核心改动

### ✅ 点赞 Set 永不过期

**修改前：**
```java
// 点赞Set有24小时过期时间
redis.sadd("moments:like:{momentId}", userId);
redis.expire("moments:like:{momentId}", 86400); // 24小时
```

**问题：**
- 24小时后 Set 被删除
- 用户刷新页面显示"未点赞"
- 但数据库里还有点赞记录
- **用户体验极差！**

---

**修改后：**
```java
// 点赞Set永不过期
redis.sadd("moments:like:{momentId}", userId);
// 不设置过期时间
```

**好处：**
- ✅ 用户点赞状态永远准确
- ✅ 不会出现"我明明点赞了但显示未点赞"的bug
- ✅ 简化逻辑，更可靠

---

## 数据库回源机制

**问题：** 如果 Redis 内存满了，LRU 策略可能会淘汰某些 key

**解决方案：** 查询点赞状态时，先查 Redis，如果没有再查数据库

```java
// 1. 先查 Redis
boolean liked = redis.sismember("moments:like:" + momentId, userId);

// 2. 如果 Redis 没有，查数据库
if (!liked) {
    liked = db.checkLike(momentId, userId);
    
    // 3. 如果数据库有，回写到 Redis
    if (liked) {
        redis.sadd("moments:like:" + momentId, userId);
    }
}
```

**流程图：**
```
用户查询点赞状态
    ↓
查询 Redis Set
    ↓
有记录? 
    ↓ 是
返回"已点赞" ✅
    ↓ 否
查询数据库
    ↓
有记录?
    ↓ 是
回写 Redis + 返回"已点赞" ✅
    ↓ 否
返回"未点赞" ✅
```

---

## 内存占用评估

**假设数据：**
- 100万个帖子
- 每个帖子平均 50 人点赞
- 每个用户 ID 占用 8 字节

**计算：**
```
1,000,000 × 50 × 8 bytes = 400 MB
```

**结论：** 对于 IM 系统来说，400MB 的内存占用**完全可以接受**。

---

## 推荐 Redis 配置

```yaml
# redis.conf
maxmemory: 4gb
maxmemory-policy: allkeys-lru  # 内存满了自动淘汰最少使用的key
```

**说明：**
- 热门帖子的点赞关系会一直保留
- 冷门帖子的点赞关系可能被淘汰
- 被淘汰后，查询时会自动从数据库回源

---

## 涉及的文件修改

### 1. Lua 脚本
**文件：** `im-moments/src/main/resources/lua/moments_like.lua`

**修改：** 删除了对点赞Set的过期时间设置
```lua
-- ❌ 删除了这些行
-- redis.call('EXPIRE', likeSetKey, likeTTL)
```

### 2. Java 代码
**文件：** `im-moments/src/main/java/com/zzzlew/server/impl/MomentsServiceImpl.java`

**修改：**
- `like()` 方法：调用 Lua 脚本时不再传递 `likeTTL` 参数
- `isMomentLiked()` 方法：添加数据库回源逻辑

### 3. Mapper 接口
**文件：** `im-moments/src/main/java/com/zzzlew/mapper/MomentsMapper.java`

**新增方法：**
```java
List<Long> getUserLikedMoments(@Param("userId") Long userId, 
                                @Param("momentIds") List<Long> momentIds);
```

### 4. Mapper XML
**文件：** `im-moments/src/main/resources/mapper/MomentsMapper.xml`

**新增 SQL：**
```xml
<select id="getUserLikedMoments" resultType="java.lang.Long">
    SELECT moment_id
    FROM moments_like
    WHERE user_id = #{userId}
    AND moment_id IN (...)
</select>
```

---

## 使用建议

### 对于小项目（用户 < 10万）
- ✅ 直接使用，不用担心内存
- ✅ 点赞关系永不过期

### 对于中大型项目（用户 > 100万）
- ✅ 配置 Redis LRU 策略
- ✅ 依赖数据库回源机制
- ⚠️ 监控 Redis 内存使用情况

### 如果内存真的不够
可以考虑：
1. 只缓存最近 30 天的帖子点赞关系
2. 使用 Redis Cluster 扩展内存
3. 冷热数据分离（热数据 Redis，冷数据 MySQL）

---

## 总结

**核心思想：** 牺牲一点内存，换取用户体验和数据准确性。

**优点：**
- ✅ 用户点赞状态永远准确
- ✅ 不会出现"点赞消失"的bug
- ✅ 代码逻辑更简单
- ✅ 配合数据库回源，即使 Redis 淘汰数据也不会出错

**缺点：**
- ⚠️ Redis 内存占用增加（但对于 IM 系统可接受）

---

**修改时间：** 2026-06-08  
**文档版本：** v1.0
