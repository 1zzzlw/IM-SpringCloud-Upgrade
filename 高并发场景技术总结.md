# IM 项目高并发场景技术总结

## 一、项目高并发场景总览

### 1.1 涉及高并发的业务模块

本项目是一个仿微信全栈即时通讯系统，基于 Spring Cloud Alibaba 微服务架构。以下模块涉及高并发场景：

| 模块 | 高并发场景 | 核心技术 |
|------|-----------|---------|
| **im-chat** | 红包发送与抢夺 | Redis Lua 原子扣减、二倍均值算法、MQ 异步到账 |
| **im-moments** | 帖子打赏 | MQ 异步削峰、Redisson 幂等锁、MySQL 行锁 |
| **im-pay** | 钱包支付/扣款/转账 | MySQL FOR UPDATE + 乐观锁 + Redisson 分布式锁（三层锁策略） |
| **im-server** | Netty 集群消息推送 | RabbitMQ 集群路由、Channel 内存映射、离线消息 Lua 存储 |
| **im-auth** | 登录 Token 竞争 | Redis Lua 原子清理旧 Token + 写入新 Token |

### 1.2 核心技术栈

```
┌─────────────────────────────────────────────────────────┐
│                    高并发技术栈                           │
├───────────────┬───────────────┬─────────────────────────┤
│  Redis Lua    │  Redisson     │  RabbitMQ               │
│  原子操作      │  分布式锁      │  异步削峰/解耦            │
├───────────────┼───────────────┼─────────────────────────┤
│  MySQL        │  雪花算法      │  Netty                  │
│  FOR UPDATE   │  全局唯一ID    │  WebSocket长连接         │
│  + 乐观锁      │               │  + CompletableFuture    │
└───────────────┴───────────────┴─────────────────────────┘
```

### 1.3 高并发复杂度评估

**比较复杂的方面：**

1. **分布式状态管理**：红包的剩余金额/数量由 Redis Hash 维护，数据库只做最终更新。Redis 与 DB 之间存在状态一致性的时间窗口，如果 Redis 在抢红包中途宕机/过期后从 DB 回灌，会覆盖实时进度导致超发。

2. **跨服务无事务协调**：红包发送跨 3 个系统（im-chat → im-pay Feign 扣款 → Redis 预热），打赏跨 2 个系统（im-moments → MQ → im-pay → MQ → im-moments），都**没有**分布式事务框架（如 Seata）或 Saga 补偿机制。

3. **多层锁的协作**：MySQL 悲观锁（FOR UPDATE）、MySQL 乐观锁（WHERE balance >= amount）、Redisson 分布式锁三层协同，但不同场景使用了不同组合，缺乏统一规范。

4. **MQ 消费者幂等性缺陷**：核心幂等锁 `DistributedLockUtil.executeIfAbsent()` 设计有误——锁在业务执行完后**立即释放**，无法阻止 MQ 消息重投导致的重复消费。

5. **Redis 缓存可靠性**：红包金额计算完全依赖 Redis 中的 Lua 脚本原子执行，但缓存预热逻辑在并发下有覆盖风险。

**不算"极高并发"的方面：**

- 未引入专门的分布式事务框架（Seata 等）
- 单 MySQL 实例，未分库分表
- 红包随机算法使用 `userId` 作为种子，不是真正随机
- 未使用专门的限流中间件（如 Sentinel 的集群限流）

---

## 二、场景一：红包（发送 + 抢夺）

### 2.1 业务流程

```
发送红包:
┌──────┐    Feign扣款     ┌──────┐    INSERT DB    ┌──────┐    PUT Redis     ┌──────┐
│ 用户  │ ─────────────→ │im-pay│ ←───────────── │im-chat│ ──────────────→ │Redis │
│ 发起  │                │ 钱包  │  扣款成功       │ 写表  │   预热Hash+Set  │缓存  │
└──────┘                └──────┘                └──────┘                 └──────┘
                             ↑
                       如果扣款失败则
                       直接返回错误

抢夺红包:
┌──────┐   hasKey?    ┌──────┐    EVAL Lua     ┌──────┐   MQ异步到账   ┌──────┐
│ 用户  │ ──────────→ │Redis │ ─────────────→ │Redis │ ────────────→ │im-pay│
│ 点击  │  缓存缺则   │缓存  │   原子扣减      │Lua   │  im.redpacket │钱包  │
└──────┘  从DB回灌    └──────┘   返回金额(分)   └──────┘  .grab        └──────┘
                             ↓
                       用户看到金额
```

### 2.2 并发挑战点

| 挑战 | 描述 |
|------|------|
| **超发** | 多人同时抢最后几个红包，总金额不能超过设定值 |
| **重复抢** | 同一用户不能抢两次 |
| **余额一致性** | Redis 中的 remain_amount 扣减必须原子 |
| **缓存与 DB 同步** | Redis 宕机/过期后从 DB 恢复不能覆盖实时进度 |
| **跨服务事务** | 扣款(im-pay) 和 红包创建(im-chat) 之间无补偿 |

### 2.3 技术方案

#### 2.3.1 Redis Lua 原子扣减

红包核心逻辑通过 [grab_red_packet.lua](im-chat/src/main/resources/lua/grab_red_packet.lua) 在 Redis 中原子执行：

```lua
-- 1. 检查红包存在且进行中
-- 2. 检查用户是否已领取 (SISMEMBER)
-- 3. 计算金额（二倍均值法 / 普通均分 / 最后一人领剩余）
-- 4. HINCRBY 减少 remain_count
-- 5. HINCRBYFLOAT 减少 remain_amount
-- 6. SADD 记录已领取用户
-- 返回：金额（分）或错误码（-1=已领, -2=已领完）
```

**原子性保证**：Redis 单线程执行 Lua 脚本，整个"检查→计算→扣减→记录"过程不会被其他客户端中断。这从根本上解决了超发和重复领取问题。

#### 2.3.2 二倍均值算法（拼手气红包）

```
avg = remainAmount / remainCount
max = avg × 2       ← 上限 = 均值的两倍
min = 1 (分)        ← 下限 = 1 分
amount = random(min, max)
```

这个算法的核心思想：**每个人抢到的金额，期望值都等于当前剩余均值，但最多不超过均值的两倍**。这样可以保证：
- 先抢的人不会把所有钱拿走（有 2 倍上限）
- 后抢的人不会什么也拿不到（最后一人拿全部剩余）
- 整体分布大致均匀但有波动

#### 2.3.3 异步到账（MQ 削峰）

抢红包成功后，**不直接写数据库扣款**，而是：

1. Redis Lua 返回金额 → 用户立即看到结果（毫秒级响应）
2. 发送 MQ 消息到 `im-redpacket-grab-queue`
3. im-pay 的 `RedPacketGrabListener` 异步消费 → 加钱包余额 → 写流水

这样红包的抢单 QPS 不依赖于数据库的写入能力。

### 2.4 当前实现分析

**优点：**
- Lua 原子脚本正确保证了单次抢夺的原子性
- MQ 异步到账降低了抢红包接口的响应延迟
- 雪花算法生成的全局唯一红包 ID 保证分布式环境下的唯一性
- Redis Hash 预热减少了首次抢红包时 Lua 脚本因 key 不存在而失败的概率

**已知问题：**

1. **[严重] warmupRedisFromDB 覆盖风险**：`RedPacketServiceImpl.warmupRedisFromDB()` 在 Redis 缓存过期后从 DB 重新加载红包数据。但 DB 中的 `remain_amount` 和 `remain_count` **只在红包被领完时才更新**，中途抢红包的进度仅存于 Redis。如果 Redis 在中途丢失（过期/重启），回灌操作会重置红包为初始状态，导致**超发**。

2. **[严重] 发红包无补偿机制**：`sendRedPacket()` 先通过 Feign 调用 im-pay 扣款，成功后再写 DB 和 Redis。如果数据库插入失败，**钱已扣但红包未创建**，用户丢钱且没有任何补偿/回滚机制。

3. **[高危] MQ 发送失败无回滚**：Lua 脚本已在 Redis 中执行（金额已扣减），但 `rabbitTemplate.convertAndSend()` 发送到账消息失败时，用户的余额永远不会增加。Redis 状态已改变无法回滚，用户看到抢到了但收不到钱。

4. **[中危] 随机数种子为 userId**：`math.randomseed(tonumber(ARGV[1]))` 使用用户 ID 作为种子，同一用户的多次抢红包得到的随机序列是确定的，破坏了"拼手气"的随机性。

5. **[低危] 二倍均值截断**：`math.floor(min + math.random() * (max - min))` 的最大值为 `max - 1`，而不是 `max`。每个红包的上限少 1 分。

6. **[低危] HINCRBYFLOAT 精度**：所有金额都是整数（分），使用 `HINCRBYFLOAT` 存在 IEEE 754 浮点累积风险。应改用 `HINCRBY`。

### 2.5 改进方向

1. **warmup 加分布式锁或 SETNX**：回灌前先尝试 `SET lock:red_packet:{id}:warmup NX EX 10`，拿到锁再回灌，避免并发覆盖。更根本的做法是每次抢红包后同步更新 DB 的 remain_amount/remain_count。

2. **发红包 Saga 补偿**：扣款成功但红包创建失败时，调用 im-pay 的退款接口。或将顺序改为"先插入 DB（status=0 pending），再扣款，再激活"。

3. **MQ 发送加 try-catch + 死信**：发送失败时将消息写入死信队列，由定时任务重试。

4. **随机种子改进**：使用 `redis.call('TIME')[1]` 或时间戳 + 红包 ID 混合作为种子。

---

## 三、场景二：打赏（帖子 Reward）

### 3.1 端到端流程

```
┌──────────┐   ① POST /reward     ┌──────────┐   ② MQ(im.reward.create)   ┌──────┐
│ 用户A     │ ──────────────────→ │im-moments│ ─────────────────────────→ │MQ    │
│ 打赏帖子  │   amount: 10.00     │          │   RewardMessageDTO        │Broker│
└──────────┘                     └──────────┘                            └──┬───┘
                                                                           │
                                                                    ③ 消费
                                                                           ↓
                                                                      ┌──────┐
                                                                      │im-pay│
                                                                      │      │
                                                                      │ ④ executeIfAbsent(idempotentKey)
                                                                      │ ⑤ transferForReward(A→B)
                                                                      │    - FOR UPDATE 锁双方钱包
                                                                      │    - deductBalance + addBalance
                                                                      │    - 写流水 type=3/4
                                                                      │ ⑥ 发结果 MQ(im.reward.result)
                                                                      └──┬───┘
                                                                         │
                                                                  ⑦ 消费结果
                                                                         ↓
                                                                      ┌──────────┐
                                                                      │im-moments│
                                                                      │          │
                                                                      │ ⑧ updateRewardAmount
                                                                      │    +10.00
                                                                      └──────────┘
```

**设计意图**：
- 打赏请求不直接调用 im-pay（避免同步阻塞），通过 MQ 异步处理
- MQ 削峰：高并发打赏不会压垮支付系统
- Redisson 幂等锁防止 MQ 消息重复消费
- MySQL FOR UPDATE 行锁保证余额扣减的串行化

### 3.2 并发挑战点

| 挑战 | 描述 |
|------|------|
| **重复消费** | MQ 消息可能被投递多次，扣款不能重复执行 |
| **余额不足** | 多人同时打赏，某个用户的余额可能被多个打赏并发扣减 |
| **跨行死锁** | 两个转账反向执行时可能互相等待 |
| **结果回调丢失** | MQ 结果消息丢失导致 moments.reward_amount 与钱包余额不一致 |
| **钱包不存在** | 被转账人从未开通钱包 |

### 3.3 技术方案

#### 3.3.1 MQ 异步削峰

打赏请求从 im-moments 发出后：
- 发布端：直接 `rabbitTemplate.convertAndSend()`，不做任何数据库操作
- 消费端（im-pay）：`RewardMessageListener` 监听 `im-reward-create-queue`
- 结果回调：im-pay 处理完后发到 `im-reward-result-queue`，im-moments 消费

这样即使瞬间有大量打赏请求，也只是堆积在 MQ 队列中，不会击穿支付服务。

#### 3.3.2 Redisson 幂等锁

```java
// 幂等键 = "reward:" + fromUserId + "_" + momentId + "_" + timestamp
boolean executed = lockUtil.executeIfAbsent("reward:" + idempotentKey, 300, () -> {
    walletService.transferForReward(fromUserId, toUserId, amount, momentId);
    // 发送成功结果到 MQ
});
```

#### 3.3.3 MySQL 三层锁策略

转账时使用三层保护：

| 层次 | 机制 | 作用 |
|------|------|------|
| 第 1 层 | `SELECT ... FOR UPDATE` | 悲观锁，事务开始时锁住双方钱包行，阻止并发读写 |
| 第 2 层 | `WHERE balance >= #{amount}` | 乐观锁，UPDATE 时二次校验余额 |
| 第 3 层 | Redisson `executeIfAbsent` | 分布式锁，防 MQ 消息重复消费 |

### 3.4 当前实现分析

**优点：**
- MQ 异步架构正确地将打赏请求与支付处理解耦
- FOR UPDATE 行锁 + 乐观 WHERE 条件提供了数据库层面的双重保护
- 幂等键设计（fromUserId + momentId + timestamp）在正常情况下能区分不同请求

**已知问题：**

1. **[严重] executeIfAbsent 幂等锁设计错误**：`DistributedLockUtil.executeIfAbsent()` 在 `finally` 块中调用 `lock.unlock()`，导致锁在业务执行完后**立即释放**。如果 MQ 消息因网络问题被重投，第二个消费者可以再次获取锁并**重复执行转账**。正确的做法是锁应该保留到 TTL 结束，或者使用 `RBucket.setIfAbsent()`（SET NX EX 语义，不手动释放）。

2. **[严重] RewardResultListener 不幂等**：im-moments 的 `RewardResultListener` 直接执行 `UPDATE moments SET reward_amount = reward_amount + amount`。如果结果消息被重复投递，同样的金额会累加多次。

3. **[高危] 收款人钱包不存在时直接抛异常**：`transferForReward` 中如果 `toWallet == null` 就抛异常。如果帖主从未开通钱包，所有对该帖主的打赏都会失败。应该在找不到钱包时自动初始化一个零余额钱包。

4. **[高危] 异常时的 ACK 策略**：`RewardMessageListener` 在 catch 中发送失败结果后执行 `basicAck`（确认消费），意味着**消息不会重试**。如果是数据库瞬时故障，打赏请求就永久丢失了。

5. **[中危] 死锁风险**：`transferForReward` 总是先锁 fromUser 再锁 toUser。如果存在反向转账（虽然当前业务没有），会形成死锁。需要在代码层面预防，例如按 userId 大小排序后加锁。

6. **[低危] 幂等键碰撞**：`{fromUserId}_{momentId}_{timestamp}` 使用毫秒时间戳。如果同一用户在 **同一毫秒** 内发起两次打赏同一帖子（快速双击），两次请求的幂等键相同，第二次会被静默跳过，用户以为成功了但实际没扣款。

### 3.5 改进方向

1. **重写 executeIfAbsent 为 SET NX EX 语义**：使用 Redisson 的 `RBucket.setIfAbsent(key, "1", Duration.ofSeconds(ttl))` 实现真正的幂等标记，不手动释放。

2. **RewardResultListener 加幂等**：在 im-moments 维护一个 Redis Set 存储已处理的幂等键，处理前先 SISMEMBER 检查。

3. **transferForReward 自动初始化钱包**：找不到 toWallet 时先 `initWallet + insertRecord` 再继续转账。

4. **错误重试策略**：区分业务错误（余额不足 → ACK，不重试）和系统错误（DB 超时 → NACK，重试 3 次）。

---

## 四、场景三：钱包支付（im-pay）

### 4.1 核心流程

im-pay 模块管理所有用户的虚拟钱包，支持以下操作：

```
充值:  用户发起 → FOR UPDATE 锁行 → addBalance → insertRecord(type=1)
提现:  用户发起 → FOR UPDATE 锁行 → 检查余额 → deductBalance → insertRecord(type=2)
扣款:  im-chat Feign调用 → FOR UPDATE 锁行 → 检查余额 → deductBalance → insertRecord(type=5)
转账:  MQ消费 → FOR UPDATE 锁双方 → deductBalance + addBalance → insertRecord(type=3+4)
```

### 4.2 并发挑战点

| 挑战 | 描述 |
|------|------|
| **余额超扣** | 并发扣款时，两个请求可能同时读到相同的余额，都判定够扣 |
| **MQ 重复** | 红包到账消息重复投递导致重复加款 |
| **部分失败** | 转账中扣款成功但加款失败，或反之 |
| **审计一致性** | 流水的 before_balance 和 after_balance 必须与实际操作一致 |

### 4.3 技术方案

#### 4.3.1 三层锁策略

```
请求到达
    │
    ▼
┌──────────────────────┐
│ 第1层: FOR UPDATE    │ ← 悲观行锁，阻止并发读写同一钱包
│ SELECT ... FOR UPDATE│    事务开始时获取，提交时释放
└──────────┬───────────┘
           ▼
┌──────────────────────┐
│ 第2层: 乐观条件       │ ← WHERE balance >= #{amount}
│ UPDATE SET balance = │    即使悲观锁失败，UPDATE 也二次校验
│ balance - amount     │    rows==0 表示余额不足
│ WHERE balance >= amt │
└──────────┬───────────┘
           ▼
┌──────────────────────┐
│ 第3层: Redisson 幂等  │ ← 仅 MQ 消费者场景
│ executeIfAbsent()    │    防消息重复投递
└──────────────────────┘
```

#### 4.3.2 关键 SQL

```sql
-- 扣款（乐观锁条件）
UPDATE wallet SET balance = balance - #{amount}
WHERE user_id = #{userId} AND balance >= #{amount}

-- 加款（无条件，始终成功）
UPDATE wallet SET balance = balance + #{amount}
WHERE user_id = #{userId}

-- 查询并锁定（悲观锁）
SELECT * FROM wallet WHERE user_id = #{userId} FOR UPDATE
```

#### 4.3.3 转账方法 transferForReward

```java
@Transactional(rollbackFor = Exception.class)
public void transferForReward(Long fromUserId, Long toUserId, BigDecimal amount, Long momentId) {
    // 1. 锁发送方 → 校验余额
    Wallet fromWallet = walletMapper.selectByUserIdForUpdate(fromUserId);
    if (fromWallet.getBalance().compareTo(amount) < 0) throw ...

    // 2. 锁接收方
    Wallet toWallet = walletMapper.selectByUserIdForUpdate(toUserId);
    if (toWallet == null) throw ...  // 见问题 4.4.3

    // 3. 乐观扣款
    walletMapper.deductBalance(fromUserId, amount);  // rows==0 → 抛异常

    // 4. 加款
    walletMapper.addBalance(toUserId, amount);

    // 5. 双写流水
    walletMapper.insertRecord(fromUserId, amount.negate(), 3, ...); // 支出
    walletMapper.insertRecord(toUserId, amount, 4, ...);            // 收入
}
```

`@Transactional` 确保整个方法是一个原子事务——任何一步失败，前面所有数据库操作全部回滚。

### 4.4 当前实现分析

**优点：**
- FOR UPDATE + 乐观 WHERE 条件提供了双重余额保护
- `@Transactional` 事务边界保证了转账的原子性
- 流水记录了 before_balance 和 after_balance 用于审计

**已知问题：**

1. **[严重] RedPacketGrabListener 无 @Transactional**：红包到账监听器中调用 `selectByUserIdForUpdate` 但没有 `@Transactional` 注解。FOR UPDATE 锁在 SELECT 执行后立即释放（自动提交），后续的 `addBalance` 和 `insertRecord` 失去了隔离性。这导致并发下：
   - 两个红包同时到账可能互相覆盖
   - before_balance 和 after_balance 记录可能不准确
   - 极端情况下余额计算错误

2. **[严重] WalletController.deduct() 无 @Transactional**：Feign 调用的扣款接口同样缺少事务注解。并发扣款时，两个请求可能同时看到余额充足（因为 SELECT FOR UPDATE 立即释放了），然后各自执行扣款，可能导致余额超扣。

3. **[高危] executeIfAbsent 设计错误**：（同第三章 3.4.1）幂等锁在业务执行后立即释放，无法阻止重复消费。

4. **[中危] 读接口使用 FOR UPDATE**：`GET /wallet/info` 和 `GET /wallet/balance` 是两个**只读查询**，却使用了 `selectByUserIdForUpdate()`（FOR UPDATE 排他锁）。高并发下，读请求会阻塞写请求，降低整体吞吐。应改用 `SELECT ... FOR SHARE`（共享锁）或普通 SELECT（接受稍旧的数据）。

5. **[低危] 转账死锁**：两个方向相反的转账同时发生时（虽然当前业务不会），先锁 fromUser 再锁 toUser 的顺序会导致死锁。

### 4.5 改进方向

1. **RedPacketGrabListener + /deduct 加 @Transactional**：补上事务注解，让 FOR UPDATE 锁覆盖到整个扣款流程。

2. **读查询改用普通 SELECT**：余额查询不需要 FOR UPDATE，用普通 SELECT 即可（允许读到略微旧的数据）。

3. **钱包不存在时自动初始化**：在转账流程中检测到 toWallet 为空时，先执行 INSERT IGNORE INTO wallet。

4. **getBalance 去重调 initWallet**：当前每次查询都调 initWallet（INSERT IGNORE），虽无功能问题但浪费一次数据库往返。可以加缓存判断。

---

## 五、场景四：Netty 集群消息路由（im-server）

### 5.1 高并发消息推送

im-server 使用 Netty WebSocket 维持与客户端的长连接，负责实时消息的推送。高并发场景包括：

- 群聊消息广播（1 条消息 → N 个群成员）
- 好友上线/下线通知
- 系统消息群发

### 5.2 集群路由策略

```
发送者 → im-server-1（本地 Channel）
           │
           ▼
    MQMessagePublish.sendToCluster()
           │
           ├── 接收者在本地 → Channel.writeAndFlush()（直接发送，不走 MQ）
           ├── 接收者在远程 → RabbitMQ(im.push.{clusterId})（MQ 转发）
           └── 接收者离线  → Redis Sorted Set（Lua 原子存储，等上线推送）
```

**关键实现**：`MQMessagePublish.sendToCluster()` 在 `CompletableFuture.runAsync()` 中异步执行，不阻塞 Netty 的 EventLoop 线程。消息通过 `imAsyncExecutor` 线程池处理。

### 5.3 并发挑战点

| 挑战 | 技术方案 |
|------|---------|
| Netty 线程不能阻塞 | `CompletableFuture.runAsync()` 异步投递 |
| 集群间消息可靠投递 | RabbitMQ 持久队列 + 持久消息 |
| 离线消息不丢失 | Redis Sorted Set（score=雪花ID） + Lua 原子存储 |
| Channel 内存映射并发安全 | `ConcurrentHashMap<Long, Channel>` |
| 消息顺序 | 离线消息按雪花 ID（时间有序）排序后推送 |

### 5.4 离线消息处理

[store_offline_message.lua](im-server/src/main/resources/store_offline_message.lua)：

```lua
-- ZADD 消息 JSON 到 user:offline:message:content:{userId}
-- score = 雪花 ID（保证按发送时间排序）
-- TTL = 20 秒
redis.call('ZADD', KEYS[1], ARGV[2], ARGV[1])
redis.call('EXPIRE', KEYS[1], ARGV[3])
return redis.call('ZCARD', KEYS[1])
```

用户上线时（`ConnectSuccessMessageHandler`），通过 Redis Pipeline 批量 ZRANGE + DEL 拉取离线消息并推送。

---

## 六、场景五：登录 Token 竞争

### 6.1 单用户高并发登录的竞态条件

在未优化前，同一账号的高并发登录会出现"旧 Token 堆积"问题：

```
旧流程（有问题）：
  请求1: 查旧Token集合 → 准备删除
  请求2: 查旧Token集合 → 看到相同的旧Token
  请求1: 删旧 → 写新Token
  请求2: 删旧 → 写新Token（可能覆盖请求1的新Token或遗漏清理）
```

根因：**查 → 删 → 写** 是多步 Redis 操作，高并发下不是原子的。

### 6.2 Lua 脚本原子解决方案

[login_token_atomic_operation.lua](im-auth/src/main/resources/login_token_atomic_operation.lua) 将所有操作封装为单个原子事务：

```lua
-- 1. 获取用户旧 Token 集合（SET）
-- 2. 逐个删除旧 Token 对应的 Access/Refresh Hash
-- 3. 删除旧 SET → 添加新 Token 到 SET + 设置 TTL
-- 4. HSET 用户信息到新 Access Token Hash + 设置 TTL
-- 5. HSET 用户信息到新 Refresh Token Hash + 设置 TTL
```

优化后，10000 并发登录下 Redis 无旧 Token 堆积，吞吐量显著提升。

---

## 七、通用技术模式总结

### 7.1 分布式锁使用规范

| 场景 | 锁类型 | 用法 |
|------|--------|------|
| MQ 幂等 | Redisson executeIfAbsent | `executeIfAbsent(key, ttl, runnable)` — SETNX 语义，不手动释放 |
| 业务互斥 | Redisson tryLock | `tryLock(key, waitTime, leaseTime, supplier)` — watch dog 自动续期 |
| 数据库行锁 | MySQL FOR UPDATE | 配合 `@Transactional`，事务提交时释放 |

**注意事项：**
- FOR UPDATE 必须在 `@Transactional` 内才有效，否则自动提交立即释放
- Redisson 锁的 key 要有业务含义的前缀，避免冲突
- `executeIfAbsent` 不应该手动 unlock（当前实现有此 bug）

### 7.2 MQ 幂等性设计模式

```
模式1: 幂等键 + 分布式锁
  生产者生成唯一 idempotentKey → 消费者加锁 → 执行业务 → 不释放锁(TTL过期自动释放)

模式2: 幂等表
  INSERT INTO idempotent_log (key) VALUES (?)  ← UNIQUE约束
  成功 → 执行业务
  失败 → 重复消息，跳过

模式3: 消息本身幂等
  UPDATE wallet SET balance = balance + 10 WHERE user_id = 1
  无论执行多少次，结果一致（前提是 before/after 不被硬编码）
```

### 7.3 缓存与数据库一致性策略

本项目使用的策略：

| 场景 | 策略 | 说明 |
|------|------|------|
| 红包 | Cache-Aside + 最终 DB 更新 | 先写 Redis（Lua），后异步同步 DB |
| 朋友圈 | Cache-Aside + Canal binlog | 修改后 Canal 删缓存，下次读重建 |
| Token | Write-Through（Lua 原子写） | 同时写 Redis，不存在 DB 不一致问题 |

### 7.4 跨服务事务协调

当前项目**没有**使用分布式事务框架。实际协调方式：

```
强一致期望但实际是最终一致：
  - 红包发送: Feign扣款(im-pay) → DB插入(im-chat) → Redis预热
    风险: 中间步骤失败无补偿，可能丢钱

异步最终一致：
  - 打赏: MQ(im-moments) → MQ(im-pay) → MQ回调(im-moments)
    风险: 回调丢失导致 moments.reward_amount 不更新

本地事务 + MQ 发件箱模式（推荐改进方向）：
  - 先将 MQ 消息写入本地 outbox 表（同一 DB 事务）
  - 定时任务扫描 outbox 表发送 MQ
  - 发送成功后删除 outbox 记录
```

---

## 八、已知问题与改进建议

### 8.1 问题清单（按严重程度排序）

| 优先级 | 编号 | 模块 | 问题 | 影响 |
|--------|------|------|------|------|
| P0 | #1 | im-pay | `executeIfAbsent` 幂等锁设计错误（业务后立即释放） | 打赏重复扣款 |
| P0 | #2 | im-pay | `RedPacketGrabListener` 缺少 `@Transactional` | 红包到账并发错误 |
| P0 | #3 | im-pay | `WalletController.deduct()` 缺少 `@Transactional` | 红包扣款并发错误 |
| P0 | #4 | im-chat | `warmupRedisFromDB` 覆盖 Redis 实时状态 | 红包超发 |
| P0 | #5 | im-chat | 发红包无补偿机制（扣款成功但创建失败） | 用户丢钱 |
| P0 | #6 | im-moments | `RewardResultListener` 不幂等 | reward_amount 重复累加 |
| P1 | #7 | im-pay | 业务失败时 ACK 消息（不重试） | 打赏请求丢失 |
| P1 | #8 | im-pay | 收款人钱包不存在时抛异常 | 打赏全部失败 |
| P1 | #9 | im-chat | MQ 发送失败无回滚 Redis | 抢到红包没收钱 |
| P1 | #10 | im-chat | DB 中途不更新 remain 字段 | Redis 过期后数据丢失 |
| P1 | #11 | im-chat | Lua `math.randomseed(userId)` 确定性随机 | 拼手气不随机 |
| P2 | #12 | im-pay | 转账死锁风险 | 极端并发下互相等待 |
| P2 | #13 | im-chat | 二倍均值公式截断 | 最大金额少 1 分 |
| P2 | #14 | im-chat | HINCRBYFLOAT 精度 | 超大红包浮点漂移 |

### 8.2 改进优先级建议

```
第一轮（修复丢钱/超发问题，预计 2-3 小时）:
  ├── 修复 executeIfAbsent → 改用 RBucket.setIfAbsent (P0 #1)
  ├── 补 @Transactional → RedPacketGrabListener + /deduct (P0 #2, #3)
  ├── warmup 加 Redis SETNX 防护 (P0 #4)
  └── RewardResultListener 加幂等检查 (P0 #6)

第二轮（修复补偿/容错，预计 2 小时）:
  ├── 发红包加 Saga 补偿 (P0 #5)
  ├── 转账自动初始化钱包 (P1 #8)
  ├── MQ 错误重试策略 (P1 #7)
  └── DB 每次抢红包同步更新 remain (P1 #10)

第三轮（优化质量，预计 1 小时）:
  ├── Lua 随机种子改进 (P1 #11)
  ├── 二倍均值公式修正 (P2 #13)
  ├── HINCRBYFLOAT → HINCRBY (P2 #14)
  └── 读接口 FOR UPDATE → FOR SHARE (P2)
```

---

> **文档说明**：本文档基于对 im-pay、im-chat、im-moments、im-server、im-auth 五个模块的全部高并发相关代码的逐行审查编写。每个场景标注了当前实现的优点和已知问题，旨在客观反映项目的技术水平和改进方向。
