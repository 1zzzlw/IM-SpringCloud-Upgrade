<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.zzzlew.mapper.UserMapper">

    <insert id="insertUserAuth" useGeneratedKeys="true" keyProperty="id">
        insert into user_auth (user_id, username, account, phone, avatar, gender)
        values (#{userId}, #{username}, #{account}, #{phone}, #{avatar}, #{gender})
    </insert>

    <select id="selectUserAuthListByUserIdList"
            resultType="com.zzzlew.domain.entity.UserAuth">
        select user_id, username, avatar from user_auth where user_id in
        <foreach collection="targetUserIdList" item="userId" open="(" close=")" separator=",">
            #{userId}
        </foreach>
    </select>

    <select id="selectUserInfoById" resultType="com.zzzlew.domain.entity.UserAuth"
            parameterType="java.lang.Long">
        select user_id, username, avatar, account, phone, gender
        from user_auth
        where user_id = #{userId}
    </select>

    <insert id="insertUserInfo">
        insert into user_info (id, username, account, password, email, phone, gender, avatar,
                               birthday, address)
        values (#{id}, #{username}, #{account}, #{password}, #{email}, #{phone}, #{gender},
                #{avatar},
                #{birthday}, #{address})
    </insert>

    <select id="getByPhoneOrAccount"
            resultType="com.zzzlew.domain.vo.UserSearchVO">
        select u.id,
               u.username,
               u.account,
               u.avatar,
               u.gender,
               f.remark,
               u.address,
               case
                   when
                       f.id is not null then 1
                   else 0 end as is_friend
        from user_info u
                 left join friend_relation f on f.user_id = #{ userId } and f.friend_id =
                                                                            u.id
        where u.phone = #{number} or u.account = #{number}
    </select>
</mapper>