## 朋友圈模块 Redis 深度优化方向

基于对 `MomentsServiceImpl` 全部 677 行代码、两个 Lua 脚本、Controller 层以及 `RedisConstant` 的逐行分析，梳理出以下可优化方向。每个方向都会先说明当前代码的现状和问题，再给出优化思路。

---

### 一、缓存与数据库的一致性（重点方向）

这是你提到最想深入的方向，也是当前代码中最值得打磨的部分。

#### 1.1 当前现状

你的缓存架构已经做得不错了：ZSet 排行榜（最新/最热）+ String 详情缓存 + Hash 计数器 + Lua 原子操作 + Pipeline 批量回填 + 随机 TTL 防雪崩。在"读多写少"的场景下，这套方案表现良好。

但目前缺少一个关键环节：**帖子被编辑或删除时，如何让缓存失效或更新**。你的代码里目前没有 `delete` 和 `update` 方法，所以这个问题还没暴露。一旦加上这些功能，就必须面对缓存一致性问题。

#### 1.2 问题的本质

缓存一致性问题的根源是：**数据库和缓存是两个独立的存储系统，对它们的写操作无法做到原子性**。

以"删除帖子"为例，无论你先删缓存还是先删数据库，都存在一个短暂的不一致窗口：

- 先删缓存再删数据库：删缓存和删数据库之间，另一个请求可能读到旧缓存。
- 先删数据库再删缓存：删数据库和删缓存之间，另一个请求可能读到旧缓存。

#### 1.3 优化方案：延迟双删

延迟双删是对"先删缓存再删数据库"方案的增强，核心思路是**删两次缓存**，把不一致窗口压到最小：

```
第一次删除缓存  →  更新数据库  →  延迟一段时间  →  第二次删除缓存
```

第一次删除缓存是为了清除旧数据。第二次删除缓存是为了清除在"更新数据库"期间被其他请求回填的旧缓存。

延迟时间通常设置为略大于一次读请求的耗时（比如 500ms ~ 1s），确保在第二次删除之前，所有可能读到旧数据并回填缓存的请求都已经完成。

**在朋友圈场景中的应用**：

```
删除帖子：
  1. 删除 Redis 中的帖子详情缓存 (moments:list:info:{id})
  2. 删除 Redis 中的计数器缓存 (moments:count:{id})
  3. 从 ZSet 排行榜中移除 (moments:list:new, moments:list:hot)
  4. 删除数据库中的帖子记录
  5. 延迟 500ms 后，再次执行步骤 1~3（清除可能被回填的旧数据）
```

延迟删除可以通过消息队列的延迟消息实现（RabbitMQ 的 TTL + 死信队列），也可以用 `CompletableFuture.delayedExecutor` 或 `ScheduledExecutorService`。

#### 1.4 优化方案：基于消息队列的异步失效

另一种更优雅的方案是利用 RabbitMQ 实现缓存失效的异步化：

```
更新数据库  →  发送 MQ 消息  →  消费者收到消息后删除缓存
```

好处是把"删除缓存"这个动作从业务主流程中解耦出来，并且可以利用 MQ 的重试机制保证缓存一定会被删除。如果删除失败，MQ 会自动重试。

#### 1.5 可以做的实践

- 实现帖子的删除和编辑功能
- 用延迟双删方案处理缓存失效，用 JMeter 压测对比"不加延迟"和"加延迟"两种方案下的不一致率
- 进一步可以用 Canal 监听 MySQL binlog，实现完全不侵入业务代码的缓存失效（适合在简历中展示架构视野）

---

### 二、当前代码中已存在的几个一致性问题

不需要等新功能，现有代码中其实已经藏着几个值得关注的点。

#### 2.1 `publish` 方法：发布帖子时不缓存详情

```java
// 当前代码 (第 86~97 行)
public void publish(MomentsDTO momentsDTO) {
    momentsMapper.insert(momentsDTO);           // 写数据库
    stringRedisTemplate.opsForZSet().add(...);  // 写 ZSet 排行榜
    // 没有写 String 详情缓存
}
```

发布后，ZSet 里有帖子 ID，但 `moments:list:info:{id}` 里没有详情。下一次 `listByNew` 查询时，会通过 `multiGet` 发现这个帖子缓存缺失，触发回填（第 160~168 行的 `missingIds` 逻辑）。功能上不会出错，但多了一次不必要的数据库查询。

优化方向：发布时顺手把详情也缓存到 String 中，减少一次回源查询。

#### 2.2 `like` 方法：Redis 和 MySQL 的一致性窗口

```java
// 当前代码 (第 391~439 行)
@Transactional(rollbackFor = Exception.class)
public void like(Long momentId) {
    Long result = stringRedisTemplate.execute(momentsLikeScript, ...);  // 1. 先改 Redis
    if (result == 1) {
        momentsMapper.like(momentId, 1);    // 2. 再改 MySQL
    } else if (result == 0) {
        momentsMapper.like(momentId, -1);   // 2. 再改 MySQL
    }
}
```

这里有一个隐含的风险：Redis Lua 脚本执行成功后，如果 MySQL 更新失败（比如数据库连接超时），`@Transactional` 会回滚 MySQL 操作，但 **Redis 的修改无法回滚**。结果就是 Redis 中的点赞数和数据库中的点赞数不一致。

这正好就是你困惑的地方——**Lua 脚本解决的是 Redis 内部多个命令的原子性，但它管不了 Redis 和 MySQL 之间的一致性**。

优化方向（选其一即可）：

- **方案 A（简单实用）**：先写 MySQL，再更新 Redis。MySQL 失败则不更新 Redis，保持一致。Redis 失败则下次查询时从 MySQL 回填。这种方案牺牲一点性能换取简单的一致性保证。
- **方案 B（推荐实践）**：保持当前的 Redis 优先策略，但把 MySQL 更新改为异步（通过 RabbitMQ 发送消息，消费者负责更新数据库）。如果消费者失败，MQ 会自动重试。这样既保证了 Redis 的低延迟响应，又通过 MQ 的重试机制保证了最终一致性。这个方案很适合做 JMeter 压测对比。

#### 2.3 `publishComment` 方法：`hasKey` 检查的竞态条件

```java
// 当前代码 (第 556~569 行)
Boolean exists = stringRedisTemplate.hasKey(countKey);
if (Boolean.TRUE.equals(exists)) {
    stringRedisTemplate.opsForHash().increment(countKey, "comment", 1);
} else {
    // 从数据库查询并预热...
}
```

`hasKey` 和后续的 `increment` 是两条独立的 Redis 命令。在极端并发下，`hasKey` 返回 `true` 后、`increment` 执行前，这个 Key 可能刚好过期了，导致 `increment` 在一个空 Hash 上执行，评论数变成 1 而不是实际值。

优化方向：把这个逻辑也写成 Lua 脚本（检查存在性 + 递增在同一个脚本中原子执行），或者直接用 `HINCRBY`，因为它在 Key 不存在时会自动创建 Hash 并设置为 0 再递增。然后配合一个独立的逻辑来校准初始值。

---

### 三、分布式锁适用场景的梳理

这是你提到比较迷糊的地方，我帮你系统地梳理一下。

#### 3.1 核心判断标准

判断是否需要分布式锁，只需要问自己一个问题：

> **我的操作是否涉及"先读取某个状态，再根据这个状态做决策"，而且这两步之间可能被其他并发请求插入？**

如果是，就需要分布式锁。如果整个操作是原子的（比如全在一条 Lua 脚本里），就不需要。

#### 3.2 你的朋友圈各功能逐一分析

| 功能 | 是否需要分布式锁 | 原因 |
|------|:---:|------|
| 点赞/取消点赞 | 不需要 | Lua 脚本在 Redis 内原子执行，判断+修改一步到位 |
| 发布朋友圈 | 不需要 | 插入数据库 + 写 ZSet 是独立操作，不存在"先读后写"的并发竞争 |
| 发布评论 | 不需要 | 插入评论 + 更新计数，不涉及基于已有状态的决策 |
| 查询朋友圈 | 不需要 | 纯读操作，不涉及写 |
| 缓存预热 | 不需要 | `moments_warmup_cache.lua` 中已有 `EXISTS` 检查，原子执行 |

#### 3.3 对比：优惠券场景为什么需要锁

```
优惠券下单流程：
  1. 查询优惠券库存（读 MySQL）    ← 读到"剩余 1 张"
  2. 判断库存是否充足（业务决策）    ← 判断"够了，可以下单"
  3. 扣减库存、创建订单（写 MySQL）  ← 执行扣减

问题：在步骤 1 和步骤 3 之间，另一个请求也读到了"剩余 1 张"，
      两个请求都认为自己可以下单，结果卖出了 2 张，超卖了。

解决方案：用分布式锁把步骤 1~3 锁起来，同一时间只有一个请求能执行。
```

```
你的点赞流程：
  1. SISMEMBER 判断是否已赞（读 Redis）
  2. SADD/SREM 修改点赞状态（写 Redis）
  3. HINCRBY 更新计数（写 Redis）
  4. ZADD 更新排行榜（写 Redis）

这 4 步全在一条 Lua 脚本里执行，Redis 单线程保证了它们不会被其他命令插入。
所以 Lua 脚本本身就等价于一把"锁"，不需要额外加分布式锁。
```

#### 3.4 什么情况下朋友圈也需要分布式锁

如果将来增加这样的功能，就需要考虑分布式锁了：

- **每人每天最多发布 5 条朋友圈**：需要"先查 MySQL 今天发了几条，再决定是否允许发布"。查和写之间有时间窗口，需要锁。
- **帖子总数限制**（比如整个平台最多 10 万条帖子）：需要先查总数再决定是否允许发布，需要锁。
- **限时抢购/限量活动帖子**：类似优惠券场景，需要锁。

总结一句话：**Lua 脚本保护的是 Redis 内部的原子性，分布式锁保护的是跨系统操作的一致性。两者不冲突，也不重叠。**

---

### 四、点赞数据的异步持久化

当前 `like` 方法是同步写 MySQL 的（`momentsMapper.like`），在高并发下 MySQL 会成为瓶颈。

#### 优化思路

点赞的核心体验是"快"，用户点完赞应该立即看到反馈，不需要等 MySQL 写完。可以这样改造：

```
用户点赞  →  Redis Lua 脚本原子操作（立即返回）  →  发送 MQ 消息  →  消费者批量更新 MySQL
```

消费者可以做一个优化：**合并同一帖子的多次点赞操作**。比如 1 秒内某个帖子收到了 50 次点赞和 10 次取消点赞，消费者只需要执行一条 SQL：`UPDATE moments SET like_count = like_count + 40 WHERE id = ?`，而不是 60 条 SQL。

这个方案很适合做压测对比：同步写 MySQL vs 异步 MQ 写，观察 TPS 和响应时间的差异。

---

### 五、评论回复功能

Controller 中的 `commentReply` 和 `publishCommentReply` 两个接口目前返回的是硬编码字符串，尚未实现。

#### 可以结合 Redis 做的优化

- 评论列表的热门评论可以用 Redis ZSet 做排序（score = 点赞数）
- 评论的点赞也可以用 Lua 脚本做原子操作（和你帖子点赞的思路完全一样）
- 热门帖子的评论列表可以缓存到 Redis List 中，减少数据库查询

---

### 六、帖子详情页的缓存穿透防护

当前 `getById` 方法（第 484~524 行）在缓存未命中时直接查数据库。如果有人恶意请求大量不存在的帖子 ID，每次都会穿透到数据库。

#### 优化方向

- **缓存空值**：查询数据库发现帖子不存在时，在 Redis 中缓存一个空值标记（如 `"NULL"`），设置较短的 TTL（如 60 秒）。后续同样的 ID 请求直接返回"不存在"，不再查数据库。
- **布隆过滤器**：在缓存层前面加一个布隆过滤器，把所有合法的帖子 ID 预先写入。请求进来先过布隆过滤器，判断"不存在"的直接拦截，判断"可能存在"的再走缓存流程。布隆过滤器的内存占用远小于缓存空值。

这两个方案可以都做，先做缓存空值（简单），再做布隆过滤器（进阶），用压测数据对比效果。

---

### 七、优化优先级建议

按照"面试展示价值"和"实现难度"排序：

| 优先级 | 方向 | 面试亮点 | 实现难度 |
|:---:|------|:---:|:---:|
| 1 | 缓存一致性（延迟双删 + 帖子删除/编辑功能） | 非常高 | 中 |
| 2 | 点赞异步持久化（Redis + MQ 异步写 MySQL） | 高 | 中 |
| 3 | `like` 方法的 Redis-MySQL 一致性改造 | 高 | 中 |
| 4 | 缓存穿透防护（空值缓存 + 布隆过滤器） | 高 | 低→中 |
| 5 | `publishComment` 的 Lua 脚本化 | 中 | 低 |
| 6 | 评论回复功能 + 评论缓存 | 中 | 中 |

建议从第 1 项开始，因为它直接对应你最想深入的"缓存一致性"主题，而且可以产出完整的压测数据。第 2~4 项每一个都可以单独作为一个优化专题来写，放在简历里都是很有说服力的点。



#  分布式锁场景

打赏功能

```
打赏操作涉及跨系统操作：
  1. 查 MySQL：用户余额是否足够
  2. 写 MySQL：扣减用户余额
  3. 写 MySQL：创建打赏记录
  4. 写 MySQL/Redis：更新帖子的打赏计数
```





